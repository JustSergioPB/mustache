import { FormFieldHintDirective } from './form-field-hint.directive';

describe('FormFieldHintDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldHintDirective();
    expect(directive).toBeTruthy();
  });
});
