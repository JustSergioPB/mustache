import { LeafToDropdownPipe } from './leaf-to-dropdown.pipe';

describe('LeafToDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new LeafToDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
